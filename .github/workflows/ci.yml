name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    name: Validate skills & docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate repository (skills, manifest, links, layout)
        run: python3 scripts/validate.py

      - name: Unit tests (memoir CLI)
        run: python3 -m unittest discover -s tests -v

      - name: Truth-contract linter eval (golden fixture)
        run: |
          # the invented-details chapter must fail a strict run ...
          if ./bin/memoir lint --workspace tests/fixtures/lint \
               --chapters tests/fixtures/lint/chapters \
               --memories tests/fixtures/lint/memories \
               --fail-on unsupported; then
            echo "FAIL: linter did not flag the invented details"; exit 1
          fi
          # ... and the faithfully-sourced chapter must pass it
          ./bin/memoir lint --workspace tests/fixtures/lint \
            --chapters tests/fixtures/lint/clean-chapters \
            --memories tests/fixtures/lint/memories \
            --fail-on any

      - name: CLI end-to-end smoke (setup + doctor in a temp workspace)
        run: |
          WS="$(mktemp -d)/ws"
          ./bin/memoir setup --workspace "$WS" --adapter claude-code --notify file
          ./bin/memoir doctor --workspace "$WS" --adapter claude-code || true
          # doctor may report missing claude/crontab on the runner; assert the
          # things the CLI itself controls:
          test -f "$WS/.memoir/autonomous-settings.json"
          test -f "$WS/.memoir/cron.txt"
          test -x "$WS/.memoir/notify.sh"

      - name: Build and install the wheel, then run it outside the checkout
        run: |
          python3 -m pip install --quiet build
          python3 scripts/stage_bundle.py
          python3 -m build --wheel
          python3 -m venv /tmp/venv
          /tmp/venv/bin/pip install --quiet dist/*.whl
          # the CLI must work with no checkout in sight: cd away from the repo
          cd /tmp
          /tmp/venv/bin/memoir --version
          WS=/tmp/pkgws
          /tmp/venv/bin/memoir setup --workspace "$WS" --adapter claude-code --notify file
          test "$(find "$WS/.claude/skills" -name SKILL.md | wc -l)" -eq 6
          /tmp/venv/bin/memoir bundle --format claude-plugin --out /tmp/bundles
          test -f /tmp/bundles/memoir-agent/.claude-plugin/plugin.json

      - name: Shell syntax check
        run: sh -n deployment/detect-runtime.sh

      - name: ShellCheck
        run: |
          sudo apt-get update -q && sudo apt-get install -y -q shellcheck
          shellcheck deployment/detect-runtime.sh

      - name: Smoke-run runtime detector (read-only)
        run: sh deployment/detect-runtime.sh
