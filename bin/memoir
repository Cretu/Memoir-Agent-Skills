#!/usr/bin/env python3
"""Launcher for the memoir CLI (usable straight from a repo clone)."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from memoir_cli.__main__ import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
